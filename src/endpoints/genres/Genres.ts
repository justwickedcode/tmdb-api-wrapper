import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/genres';
export class Genres extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
