import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/discover';
export class Discover extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
