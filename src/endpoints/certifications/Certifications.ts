import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/certifications';
export class Certifications extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
