import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/watch/providers';
export class WatchProviders extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
