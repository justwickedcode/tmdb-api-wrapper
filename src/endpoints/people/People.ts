import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/people';
export class People extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
