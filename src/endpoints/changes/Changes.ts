import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/changes';
export class Changes extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
