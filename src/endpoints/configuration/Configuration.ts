import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/configuration';
export class Configuration extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
