import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/keywords';
export class Keywords extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
