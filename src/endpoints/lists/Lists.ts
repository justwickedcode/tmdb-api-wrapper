import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/lists';
export class Lists extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
