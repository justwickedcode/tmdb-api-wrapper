import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/trending';
export class Trending extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
