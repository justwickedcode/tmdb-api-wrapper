import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/find';
export class Find extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
