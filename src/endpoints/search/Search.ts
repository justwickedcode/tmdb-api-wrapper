import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/search';
export class Search extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
