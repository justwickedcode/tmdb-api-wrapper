import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/reviews';
export class Reviews extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
