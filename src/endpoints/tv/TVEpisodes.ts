import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/tv';
export class TVEpisodes extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
