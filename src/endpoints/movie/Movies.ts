import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/movie';
export class Movies extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
