import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/companies';
export class Companies extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
