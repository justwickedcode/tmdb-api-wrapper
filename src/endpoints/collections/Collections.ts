import { BaseSectionEndpoint } from '../BaseSectionEndpoint';

const BASE_PATH = '/collections';
export class Collections extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
