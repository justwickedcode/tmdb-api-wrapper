import HTTPClient from '../../HTTPClient';

const BASE_PATH = '/networks';
export class Networks {
  private readonly http: HTTPClient;

  constructor(http: HTTPClient) {
    this.http = http;
  }

  // AccountEndpoints
}
