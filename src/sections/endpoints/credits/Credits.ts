import { BaseSectionEndpoint } from '@services/src/sections/endpoints/BaseSectionEndpoint';

const BASE_PATH = '/credits';
export class Credits extends BaseSectionEndpoint {
  constructor(accessToken: string) {
    super(accessToken);
  }

  // Endpoints
}
